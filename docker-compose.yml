version: "3.7"
services:
  phpunit:
    container_name: phpunit
    image: prestashop/docker-internal-images:nightly
    working_dir: /var/www/html/modules/ps_accounts
    environment:
      - PS_DOMAIN=localhost
      - PS_ENABLE_SSL=0
      - PS_DEV_MODE=1
    volumes:
      - ./code/ps_accounts:/var/www/html/modules/ps_accounts
    entrypoint: [
        "/bin/sh", "-c", " \
          service mysql start && \
          php -d memory_limit=-1 ../../bin/console prestashop:module install ps_accounts && \
          chown -R www-data:www-data ../../var/logs && \
          apache2-foreground
        "
    ]
